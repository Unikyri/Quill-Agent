import PageStatus from '../components/shared/PageStatus'

// ponytail: stub — full implementation in PR 3 (Plot-holes)
export default function PlotHolesPage() {
  return <PageStatus loading={true} />
}
