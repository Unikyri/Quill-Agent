import PageStatus from '../components/shared/PageStatus'

// ponytail: stub — full implementation in PR 2 (Knowledge Graph)
export default function KnowledgeGraphPage() {
  return <PageStatus loading={true} />
}
